{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "editor debug server",
      "request": "launch",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["dev", "--host", "0.0.0.0"],
      "type": "node",
      "cwd": "${workspaceFolder}/editor"
    },
    {
      "name": "app debug server",
      "request": "launch",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["dev", "--host", "0.0.0.0"],
      "type": "node",
      "cwd": "${workspaceFolder}"
    }
  ]
}
